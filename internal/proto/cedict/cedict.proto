syntax = "proto3";

option go_package = "internal/proto/cedict";

message Dict {
	map<string, Entries> simplified  = 1;
	map<string, bool> syllables = 2;
}

message Entries {
    map<string, Entry> entries = 1;
}

message Entry {
    repeated string entry = 1;
}
